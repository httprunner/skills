interface:
  display_name: "Group Webhook Dispatch"
  short_description: "Dispatch/reconcile group webhooks and upsert plans."
  default_prompt: "Use $group-webhook-dispatch to dispatch by task-id/group-id, reconcile pending/failed by date, and upsert webhook plans into WEBHOOK_BITABLE_URL."
